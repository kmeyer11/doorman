# Copy this file to .env and fill in the keys for whichever provider(s) you use.
# .env is gitignored — never commit real keys.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Free tier via Google AI Studio: https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Optional: override the default heuristic thresholds (0-100 heuristic score).
# DOORMAN_FLAG_THRESHOLD=25
# DOORMAN_BLOCK_THRESHOLD=60
